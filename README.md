# lob-fill-engine
Limit order fill probability estimator trained on LOBSTER tick data — models intraday microstructure dynamics using order book imbalance, queue depth, and time-of-day features with walk-forward cross-validation
